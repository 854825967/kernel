totalnum = 2

// DB前端配置
dbserver config
{
	// DB前端ID
	DBServerID = 1
	// DB前端IP地址
	DBServerIP = [db_server_ip]
	// DB前端端口
	DBServerPort = 8900
}

// DB前端配置
dbserver config
{
	// DB前端ID
	DBServerID = 2
	// DB前端IP地址
	DBServerIP = [db_server_ip]
	// DB前端端口
	DBServerPort = 8901
}
