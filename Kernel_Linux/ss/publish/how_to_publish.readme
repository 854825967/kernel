版本发布方法：

1.完整版本发布：
  执行：
  $ ./publish.sh
  生成publish.tar.gz文件，就是完成版本的压缩包。

2.补丁版本发布：
  执行：
  $ ./publish.sh 1209 1230
  该shell打包svn版本[r1209到r1230]之间的所有修改，生成publish_patch.tar.gz文件，就是版本的补丁压缩包。

小版本的发布都使用补丁包的形式。
